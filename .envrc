# direnv: auto-activate the mise toolchain (node, go) when you cd into this project.
# Requires direnv hooked into your shell and `direnv allow` once.
# `use mise` reads mise.toml, puts the pinned tools on PATH, and reloads on changes.
use mise
